version: '3.8'

services:
  taskrouter-mcp:
    build: .
    container_name: taskrouter-mcp
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
    stdin_open: true
    tty: true
    volumes:
      - ./logs:/app/logs
